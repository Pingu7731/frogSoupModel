syntax = "proto3";

package frogsoup.game;

import "../../proto/game/game/room.proto";

// Defines a level (world) that contains multiple rooms
message FrogsoupLevelProto {
  string id = 1;
  repeated RoomProto rooms = 2;
}
